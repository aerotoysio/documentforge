<Window x:Class="DocumentForge.Studio.Views.ConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect to DocumentForge"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Saved connections -->
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Saved:" VerticalAlignment="Center" Width="90"/>
            <Button x:Name="ForgetButton" DockPanel.Dock="Right" Content="Forget" Padding="10,2" Margin="8,0,0,0"
                    Click="OnForgetSaved" IsEnabled="False"/>
            <ComboBox x:Name="SavedCombo" SelectionChanged="OnSavedSelected" DisplayMemberPath="Name"/>
        </DockPanel>

        <!-- Kind -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Connect to:" VerticalAlignment="Center" Width="90"/>
            <RadioButton x:Name="FileRadio" Content="Database file (.dfdb)" IsChecked="True"
                         Checked="OnKindChanged" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <RadioButton x:Name="HttpRadio" Content="Server / endpoint (HTTP)"
                         Checked="OnKindChanged" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- File fields -->
        <DockPanel Grid.Row="2" x:Name="FilePanel" Margin="0,0,0,12">
            <TextBlock Text="File:" VerticalAlignment="Center" Width="90"/>
            <Button Content="Browse…" DockPanel.Dock="Right" Padding="10,2" Margin="8,0,0,0" Click="OnBrowseFile"/>
            <TextBox x:Name="FilePathBox"/>
        </DockPanel>

        <!-- HTTP fields -->
        <StackPanel Grid.Row="3" x:Name="HttpPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <DockPanel Margin="0,0,0,8">
                <TextBlock Text="URL:" VerticalAlignment="Center" Width="90"/>
                <TextBox x:Name="UrlBox" Text="http://localhost:5001"/>
            </DockPanel>
            <DockPanel>
                <TextBlock Text="API key:" VerticalAlignment="Center" Width="90"/>
                <PasswordBox x:Name="ApiKeyBox"/>
            </DockPanel>
            <TextBlock Text="Paste just the key (hex or df_…) — 'Bearer' is added automatically. Leave empty for a dev-mode node (no auth configured)."
                       Foreground="Gray" FontSize="11" Margin="90,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Name + save -->
        <DockPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="Name:" VerticalAlignment="Center" Width="90"/>
            <CheckBox x:Name="SaveCheck" DockPanel.Dock="Right" Content="Save connection" IsChecked="True"
                      VerticalAlignment="Center" Margin="12,0,0,0"/>
            <TextBox x:Name="NameBox"/>
        </DockPanel>

        <!-- Test status -->
        <TextBlock Grid.Row="5" x:Name="TestStatus" Margin="90,0,0,12" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TestButton" Content="Test" Width="80" Margin="0,0,8,0" Click="OnTest"/>
            <Button x:Name="ConnectButton" Content="Connect" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OnConnect"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
