<UserControl x:Class="DocumentForge.Studio.Views.TopologyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DocumentForge.Studio.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:TopologyDocumentViewModel}">
    <DockPanel>
        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar>
                <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Padding="8,2"/>
                <Separator/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="4,0"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Legend + footnote -->
        <StackPanel DockPanel.Dock="Bottom" Margin="10,6">
            <StackPanel Orientation="Horizontal">
                <Border Width="14" Height="14" Background="#E8F0FE" BorderBrush="#2A5DB0" BorderThickness="1" VerticalAlignment="Center"/>
                <TextBlock Text="Leader" Margin="4,0,16,0" VerticalAlignment="Center"/>
                <Border Width="14" Height="14" Background="#E6F7EC" BorderBrush="#2E8B57" BorderThickness="1" VerticalAlignment="Center"/>
                <TextBlock Text="Follower" Margin="4,0,16,0" VerticalAlignment="Center"/>
                <Border Width="14" Height="14" Background="#F2F2F2" BorderBrush="#999999" BorderThickness="1" VerticalAlignment="Center"/>
                <TextBlock Text="Standalone" Margin="4,0,16,0" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Foreground="Gray" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"
                       Text="Peer boxes labelled &quot;replication link&quot; show a replication TCP socket (an ephemeral port), not a connectable HTTP endpoint — don't try to connect to them. Left-column boxes are this node's databases."/>
        </StackPanel>

        <!-- Graph -->
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#FAFAFA">
            <Grid Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}">

                <!-- Edges (behind) -->
                <ItemsControl ItemsSource="{Binding Edges}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Line X1="{Binding X1}" Y1="{Binding Y1}" X2="{Binding X2}" Y2="{Binding Y2}"
                                  Stroke="#B0B8C4" StrokeThickness="2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Edge labels -->
                <ItemsControl ItemsSource="{Binding Edges}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding LabelX}"/>
                            <Setter Property="Canvas.Top" Value="{Binding LabelY}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}" FontSize="10" Foreground="#667"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Nodes (front) -->
                <ItemsControl ItemsSource="{Binding Nodes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><Canvas/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="{Binding Width}" Height="{Binding Height}"
                                    Background="{Binding Fill}" BorderBrush="{Binding Stroke}" BorderThickness="2"
                                    CornerRadius="6" Padding="10,6">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Subtitle}" FontSize="11" Foreground="#555"
                                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</UserControl>
