<UserControl x:Class="DocumentForge.Studio.Views.ClusterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DocumentForge.Studio.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ClusterDocumentViewModel}">
    <DockPanel>
        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar>
                <Button Content="💾 Save" Command="{Binding SaveCommand}" Padding="8,2"/>
                <Button Content="⬇ Export…" Command="{Binding ExportCommand}" Padding="8,2"
                        ToolTip="Write a copy of this cluster config to another file"/>
                <Separator/>
                <Button Content="🩺 Check health" Command="{Binding CheckHealthCommand}" Padding="8,2"/>
                <Button Content="📤 Push to nodes" Command="{Binding PushToNodesCommand}" Padding="8,2"
                        ToolTip="PUT this config to every node it names (leaders + followers) so the whole cluster agrees on it"/>
                <Separator/>
                <TextBlock Text="{Binding SummaryText}" VerticalAlignment="Center" Margin="4,0"/>
            </ToolBar>
        </ToolBarTray>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Margin="12,4" TextWrapping="Wrap"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="14">
                <!-- Load the live config from a running router instead of hunting for the file -->
                <Border Background="#EEF4FF" BorderBrush="#B9D0F5" BorderThickness="1" CornerRadius="4"
                        Padding="10,7" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="Load from a running cluster" FontWeight="SemiBold"/>
                        <TextBlock Foreground="Gray" FontSize="11" Margin="0,2,0,6" TextWrapping="Wrap"
                                   Text="Pull the live config straight from a running dfdb router (its GET /cluster/config) so you edit what the cluster is actually running. Use 'Push to nodes' to distribute your edits to every DF instance the config names."/>
                        <StackPanel Orientation="Horizontal">
                            <ComboBox IsEditable="True" Width="240"
                                      Text="{Binding RouterEndpoint, UpdateSourceTrigger=PropertyChanged}"
                                      ItemsSource="{Binding ShardEndpointSuggestions}"
                                      ToolTip="Endpoint of a running `dfdb router`, e.g. http://localhost:5100"/>
                            <Button Content="⤓ Load live config" Command="{Binding LoadFromRouterCommand}"
                                    Padding="10,2" Margin="8,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Rebalance: coordinator-driven plan preview + execute + progress (issue #116) -->
                <Border Background="#FFF7EC" BorderBrush="#F0D9B5" BorderThickness="1" CornerRadius="4"
                        Padding="10,7" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="Rebalance the cluster" FontWeight="SemiBold"/>
                        <TextBlock Foreground="Gray" FontSize="11" Margin="0,2,0,6" TextWrapping="Wrap"
                                   Text="Reshape the cluster to THIS config: a coordinator node plans the migration from its stored cluster.json (the running topology) to this editor's config, shows the per-shard move counts, then executes it. The migration is offline — run it in a maintenance window."/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Coordinator:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox IsEditable="True" Width="240"
                                      Text="{Binding RebalanceCoordinator, UpdateSourceTrigger=PropertyChanged}"
                                      ItemsSource="{Binding ShardEndpointSuggestions}"
                                      ToolTip="A DF node that can reach every shard — usually one of the shards themselves"/>
                            <Button Content="🔍 Preview plan" Command="{Binding PreviewRebalanceCommand}"
                                    Padding="10,2" Margin="8,0,0,0"/>
                            <Button Content="▶ Execute" Command="{Binding ExecuteRebalanceCommand}"
                                    IsEnabled="{Binding RebalanceCanExecute}" Padding="10,2" Margin="8,0,0,0"/>
                        </StackPanel>
                        <TextBox Text="{Binding RebalancePlanText, Mode=OneWay}" IsReadOnly="True" Margin="0,8,0,0"
                                 FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" MaxHeight="180"
                                 VerticalScrollBarVisibility="Auto" Background="#FAFAFA" BorderBrush="#F0D9B5"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Shards" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock Foreground="Gray" FontSize="11" Margin="0,2,0,6" TextWrapping="Wrap"
                           Text="Each shard is a node (or a leader+followers replication group). Data is spread across shards by the consistent-hash ring."/>
                <DataGrid ItemsSource="{Binding Shards}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" CanUserAddRows="False" MaxHeight="200"
                          GridLinesVisibility="Horizontal" FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="140"/>
                        <DataGridTextColumn Header="Leader endpoint" Binding="{Binding LeaderEndpoint}" Width="*"/>
                        <DataGridTextColumn Header="Followers" Binding="{Binding FollowerCount}" Width="80"/>
                        <DataGridTextColumn Header="Health" Binding="{Binding Health}" Width="200"/>
                        <DataGridTemplateColumn Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Padding="4,0" Margin="2"
                                            Command="{Binding DataContext.RemoveShardCommand,
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBox Text="{Binding NewShardName, UpdateSourceTrigger=PropertyChanged}" Width="140"
                             ToolTip="Shard name"/>
                    <ComboBox IsEditable="True" Text="{Binding NewShardEndpoint, UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding ShardEndpointSuggestions}" Width="240" Margin="8,0,0,0"
                              ToolTip="Leader endpoint — pick a saved server or type one, e.g. http://shard-a:5001"/>
                    <Button Content="Add shard" Command="{Binding AddShardCommand}" Padding="10,2" Margin="8,0,0,0"/>
                </StackPanel>

                <TextBlock Text="Collection sharding" FontSize="15" FontWeight="SemiBold" Margin="0,20,0,0"/>
                <TextBlock Foreground="Gray" FontSize="11" Margin="0,2,0,6" TextWrapping="Wrap"
                           Text="hash = partitioned across shards by a shard key (e.g. orders by customerId). replicated = a full copy on every shard (small reference tables, so joins stay local)."/>
                <DataGrid ItemsSource="{Binding Collections}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" CanUserAddRows="False" MaxHeight="200"
                          GridLinesVisibility="Horizontal" FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Collection" Binding="{Binding Name}" Width="180"/>
                        <DataGridTextColumn Header="Strategy" Binding="{Binding Strategy}" Width="120"/>
                        <DataGridTextColumn Header="Shard key" Binding="{Binding ShardKeyPath}" Width="*"/>
                        <DataGridTemplateColumn Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Padding="4,0" Margin="2"
                                            Command="{Binding DataContext.RemoveCollectionCommand,
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <ComboBox IsEditable="True" Text="{Binding NewCollectionName, UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding CollectionSuggestions}" Width="180"
                              ToolTip="Collection name — suggestions come from healthy shards after a health check"/>
                    <ComboBox ItemsSource="{Binding Strategies}" SelectedItem="{Binding NewCollectionStrategy}"
                              Width="120" Margin="8,0,0,0"/>
                    <TextBox Text="{Binding NewCollectionShardKey, UpdateSourceTrigger=PropertyChanged}" Width="160" Margin="8,0,0,0"
                             IsEnabled="{Binding NeedsShardKey}"
                             ToolTip="Shard key path — required for hash, ignored for replicated (e.g. customerId)"/>
                    <Button Content="Add / update" Command="{Binding AddCollectionCommand}" Padding="10,2" Margin="8,0,0,0"/>
                </StackPanel>

                <Border Background="#FFF8E6" BorderBrush="#F0E0A0" BorderThickness="1" CornerRadius="4"
                        Padding="10,7" Margin="0,20,0,0">
                    <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#665500">
                        Studio edits your cluster config and health-checks shards. The router reads this config at
                        startup, so applying changes means saving the file and restarting the router (a live push
                        endpoint is tracked). Known engine caveats: cross-shard AVG is currently inaccurate (#99) and
                        the CLI router uses a different hash than the engine ring (#100).
                    </TextBlock>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
