<UserControl x:Class="DocumentForge.Studio.Views.ApiKeysView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DocumentForge.Studio.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ApiKeysDocumentViewModel}">
    <DockPanel Margin="14">
        <TextBlock DockPanel.Dock="Top" Text="API Keys &amp; security" FontSize="18" FontWeight="Light"/>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Margin="0,8,0,0" TextWrapping="Wrap"/>

        <!-- Security state banner (shown when this connection has no key = open server) -->
        <Border DockPanel.Dock="Top" Background="#FDECEA" BorderBrush="#C0392B" BorderThickness="1"
                CornerRadius="5" Padding="12,8" Margin="0,10,0,0"
                Visibility="{Binding IsUnsecured, Converter={StaticResource BoolToVis}}">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="🔒 Secure this server" Padding="12,4" Margin="10,0,0,0"
                        VerticalAlignment="Center" Command="{Binding SecureCommand}"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="⚠ This server is not secured" FontWeight="SemiBold" Foreground="#C0392B"/>
                    <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#7A2018" Margin="0,2,0,0"
                               Text="No API key is set, so anyone who can reach it can read and write all data. Click 'Secure this server' to mint an admin key, apply it to this connection, and reconnect (no lock-out)."/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- One-time secret banner -->
        <Border DockPanel.Dock="Top" Background="#E6F7EC" BorderBrush="#2E8B57" BorderThickness="1"
                CornerRadius="5" Padding="12,8" Margin="0,10,0,0"
                Visibility="{Binding CreatedSecret, Converter={StaticResource NonEmptyToVis}}">
            <StackPanel>
                <TextBlock Text="New key secret — copy it now, it cannot be retrieved again:" FontWeight="SemiBold"/>
                <DockPanel Margin="0,6,0,0">
                    <Button DockPanel.Dock="Right" Content="Copy" Padding="10,2" Margin="8,0,0,0" Command="{Binding CopySecretCommand}"/>
                    <Button DockPanel.Dock="Right" Content="Dismiss" Padding="10,2" Margin="8,0,0,0" Command="{Binding DismissSecretCommand}"/>
                    <TextBox Text="{Binding CreatedSecret, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas"
                             VerticalContentAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>

        <!-- Keys Studio knows on this machine (reveal/copy). The server's key
             list below can never show secrets — it stores hashes only. -->
        <Border DockPanel.Dock="Top" BorderBrush="#E4E4E4" BorderThickness="1" CornerRadius="5" Padding="12" Margin="0,10,0,0"
                Visibility="{Binding HasAnyKnownKey, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="Keys on this machine" FontWeight="SemiBold"/>
                <TextBlock FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,2,0,6"
                           Text="The server stores only key hashes, so the list below can never reveal a secret. These are the secrets Studio itself holds — reveal or copy them to connect other clients."/>
                <DockPanel Visibility="{Binding HasConnectionKey, Converter={StaticResource BoolToVis}}" Margin="0,2,0,0">
                    <TextBlock Text="This connection's key:" Width="160" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="Copy" Padding="10,2" Margin="8,0,0,0"
                            Command="{Binding CopyConnectionKeyCommand}"/>
                    <Button DockPanel.Dock="Right" Content="👁 Show / hide" Padding="10,2" Margin="8,0,0,0"
                            Command="{Binding ToggleConnectionKeyCommand}"/>
                    <TextBox Text="{Binding ConnectionKeyDisplay, Mode=OneWay}" IsReadOnly="True"
                             FontFamily="Consolas" VerticalContentAlignment="Center"/>
                </DockPanel>
                <DockPanel Visibility="{Binding HasServiceKey, Converter={StaticResource BoolToVis}}" Margin="0,6,0,0">
                    <TextBlock Text="Installed service key:" Width="160" VerticalAlignment="Center"
                               ToolTip="The admin key the installer provisioned for the bundled local service"/>
                    <Button DockPanel.Dock="Right" Content="Copy" Padding="10,2" Margin="8,0,0,0"
                            Command="{Binding CopyServiceKeyCommand}"/>
                    <Button DockPanel.Dock="Right" Content="👁 Show / hide" Padding="10,2" Margin="8,0,0,0"
                            Command="{Binding ToggleServiceKeyCommand}"/>
                    <TextBox Text="{Binding ServiceKeyDisplay, Mode=OneWay}" IsReadOnly="True"
                             FontFamily="Consolas" VerticalContentAlignment="Center"
                             ToolTip="{Binding ServiceKeyFilePath}"/>
                </DockPanel>
                <TextBlock FontSize="10" Foreground="Gray" Margin="160,2,0,0"
                           Text="{Binding ServiceKeyFilePath, StringFormat=File: {0}}"
                           Visibility="{Binding HasServiceKey, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Border>

        <!-- Create -->
        <Border DockPanel.Dock="Top" BorderBrush="#E4E4E4" BorderThickness="1" CornerRadius="5" Padding="12" Margin="0,10,0,10">
            <StackPanel>
                <TextBlock Text="Create a key" FontWeight="SemiBold"/>
                <TextBlock Foreground="#B00" FontSize="11" Margin="0,2,0,8" TextWrapping="Wrap"
                           Text="⚠ On a dev-mode server (no auth yet), creating the FIRST key immediately locks the server down. Use a Full-admin key (or 'Secure this server' above) so you don't lose access."/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Access:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding ScopeChoices}" SelectedItem="{Binding SelectedScope}"
                              VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="2" Text="Database:" VerticalAlignment="Center" Margin="12,0,6,0"
                               Visibility="{Binding NeedsDatabase, Converter={StaticResource BoolToVis}}"/>
                    <ComboBox Grid.Column="3" ItemsSource="{Binding Databases}" SelectedItem="{Binding SelectedDatabase}"
                              VerticalAlignment="Center"
                              Visibility="{Binding NeedsDatabase, Converter={StaticResource BoolToVis}}"/>

                    <TextBlock Grid.Column="4" Text="Description:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                    <TextBox Grid.Column="5" Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             ToolTip="A label to remember what this key is for (optional)"/>
                    <Button Grid.Column="6" Content="Create key" Padding="12,3" Margin="8,0,0,0"
                            Command="{Binding CreateCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Existing keys -->
        <DockPanel>
            <Button DockPanel.Dock="Top" HorizontalAlignment="Left" Content="🔄 Refresh" Padding="8,2" Margin="0,0,0,6"
                    Command="{Binding RefreshCommand}"/>
            <DataGrid ItemsSource="{Binding Keys}" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" CanUserAddRows="False" GridLinesVisibility="Horizontal" FontSize="12">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="220" FontFamily="Consolas"/>
                    <DataGridTextColumn Header="Scopes" Binding="{Binding Scopes}" Width="*"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="180"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="Auto"/>
                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Revoke" Padding="4,0" Margin="2"
                                        Command="{Binding DataContext.RevokeCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </DockPanel>
</UserControl>
